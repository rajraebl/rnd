@model IEnumerable<MVC4CustomFilter.Models.Emp>
@{
    ViewBag.Title = "Home Page";
}
@section featured {
    <section class="featured">
        <div class="content-wrapper">
            <hgroup class="title">
                <h1>@ViewBag.Title .xxx</h1>
                <h2>@ViewBag.Message</h2>
            </hgroup>
            <p>
            </p>
        </div>
    </section>
}
<h3>We suggest the following:</h3>
<ol class="round">
   @foreach(var e in Model)
   {
       <li>@e.name</li>
   }
</ol>
